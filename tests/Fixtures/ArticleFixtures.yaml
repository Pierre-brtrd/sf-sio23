include:
    - TagFixtures.yaml
    - UserFixtures.yaml

App\Entity\Article:
    article{1..20}:
        title (unique): <word(2)>
        content: <paragraphs(4, true)>
        enabled: <boolean()>
        categories: <numberBetween(1, 10)>x @categorie*
        author: <randomElement([@userAdmin, <randomElement(@user{1..10})>])>
        createdAt: <generateDate()>
        # images: <numberBetween(1, 3)>x <uploadImageArticle()>
