{{ form_start(form) }}
{{ form_widget(form) }}
<div class="text-center">
	<button type="submit" class="btn btn-primary text-center">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
