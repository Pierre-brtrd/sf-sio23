{% for image in article.images|slice(0,1) %}
	<div class="banner-image" style="background-image: url('{{ vich_uploader_asset(image) }}')">
		<div class="banner-header">
			<h1>{{ article.title }}</h1>
		</div>
	</div>
{% endfor %}
